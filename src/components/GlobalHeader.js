import React from 'react';
import styles from '../styles/GlobalHeader.scss';

const GlobalHeader = () => {
  return (
    <div className={styles['g-header']}>
      Header
    </div>
  );
};

GlobalHeader.propTypes = {
};

export default GlobalHeader;
